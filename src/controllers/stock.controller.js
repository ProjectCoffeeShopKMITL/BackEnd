const pool = require("../db");

// CRUD

//(GET) get all stocks list
//(POST) add stock {ingredient_name, quantity}
//(PUT) update stock

//(GET) get recipe(all need ingredient) of a menu
const getAllrecipes = async (req, res) =>
//(POST) 

module.exports = {
};